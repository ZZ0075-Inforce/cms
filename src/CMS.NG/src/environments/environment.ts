/**
 * Production environment. `ng build` uses this file as-is; `ng serve` / `ng test` swap in
 * environment.development.ts via the fileReplacements block in angular.json.
 */
export const environment = {
  production: true,
  apiBaseUrl: '/api'
};
